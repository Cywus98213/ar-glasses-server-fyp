# Core dependencies (torch installed separately for speed)
numpy>=1.21.0
soundfile>=0.10.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Audio processing
pyannote.audio>=3.1.0
faster-whisper>=0.10.0
speechbrain>=0.5.16

# Hugging Face
transformers>=4.40.0
accelerate>=0.20.0

# WebSocket server
websockets>=10.0
